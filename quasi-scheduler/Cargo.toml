[package]
name = "quasi-scheduler"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Heterogeneous resource scheduler for QUASI — CPU/GPU/QPU scheduling"

[dependencies]
hal-contract = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
