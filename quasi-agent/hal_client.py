'''HAL Contract client bindings for IBM/IQM backends.

Provides minimal functions to submit a QASM3 program and query job status.
'''\n\nimport requests\nfrom typing import Any, Dict\n\ndef submit_job(qasm_str: str, backend: str) -> Dict[str, Any]:\n    """Submit a QASM3 program to a HAL Contract endpoint.\n\n    Args:\n        qasm_str: The QASM3 source code.\n        backend: Identifier of the backend (e.g., ``ibm`` or ``iqm``).\n\n    Returns:\n        Parsed JSON response from the server.\n    """\n    payload = {\n        "qasm": qasm_str,\n        "backend": backend,\n        "shots": 1024,\n    }\n    url = f"https://{backend}.halcontract.org/jobs"\n    response = requests.post(url, json=payload)\n    response.raise_for_status()\n    return response.json()\n\ndef get_job_status(job_id: str) -> Dict[str, Any]:\n    """Retrieve the status of a previously submitted job.\n\n    Args:\n        job_id: Identifier returned by ``submit_job``.\n\n    Returns:\n        Parsed JSON response containing at least a ``status`` field.\n    """\n    url = f"https://halcontract.org/jobs/{job_id}/status"\n    response = requests.post(url)\n    response.raise_for_status()\n    return response.json()\n